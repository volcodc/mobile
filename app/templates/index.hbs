<div class="category_header">
	<h3>Top Headlines</h3>
</div>
<ul>
{{#each story in Stories}}
	{{#if @first}}
		<li>First Story: {{story.Headline}}</li>
	{{else}}
		<li>{{story.Headline}}</li>
	{{/if}}
{{/each}}
</ul>